spring.application.name=jpa-repository

# Localhost properties (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/db?useUnicode=yes&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=

# Automatically creating(dropping if exists)/updating table in database
spring.jpa.hibernate.ddl-auto=update